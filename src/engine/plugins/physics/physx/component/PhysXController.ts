import Vector3, { Vector } from '@ecs/plugins/math/Vector';

export class PhysXController {
	controller: PhysX.PxController;

	// move(displacement: Vector, minDistance: number, elapsedTime: number): void {

	// }
	// velocity: Vector3 = new Vector3();
}
